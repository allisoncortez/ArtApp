<h1> Login </h1>

<%= flash[:message] if flash[:message]%>

<%=  form_for :user do |f| %>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <br>
  <br>
  <%= f.label :password %>
  <%= f.password_field :password %>
   <br>
   <br>
  <%= f.submit "LOGIN" %>

<% end %>

<%# 
form_for: if we WERE creating a new user... 
accepts :user
#don't use an instance variable BC we're not creating new user... %>